// Barrel for the mockup kit. `@/components/mockup-kit` used to be one 305-line
// file; it is now this directory, and every existing import keeps working.
export { Hero, SectionHeader } from "./layout";
export { CommandSearch, QuickActionRow, ViewToggle, FilterChip, type QuickAction } from "./controls";
export { StatTile, Badge } from "./display";
