# VPS Ops Report → alpha · 2026-07-11

> From: **vps** (ops agent, AGENTS.md §4) · To: **alpha** (integrator) · Backend verified live this session.
> Scope: finish any open vps work, reconcile ops reality, hand alpha the board/contract changes that are alpha's to apply.

## TL;DR

- **No vps task is executable.** v1.2 already deployed (commit `3880413`, smoke HTTP 200); backend runs on **Convex Cloud `rare-toucan-552`** — no VPS/self-hosted stack left to touch. Rotation collapsed to one owner-gated secret.
- **vps applied** (this handoff): reconciled `docs/DEPLOY.md` to Convex Cloud + updated the ops rows/runbook in `docs/STATUS.md` + this report.
- **alpha to apply** (contract/brand surfaces — not vps's to rewrite): self-hosted → Cloud facts in `AGENTS.md` §2/§4/§9, `README.md`, `docs/PRD.md`. Precise edits in §5 below.
- **Owner (Rahman) only, still open:** rotate `AUTH_GOOGLE_SECRET` (Google Console). Everything else in rotation #12 is done or N/A.

## 1. vps task status

| Board row | Board says | Reality (verified 2026-07-11) | Outcome |
|---|---|---|---|
| #19 deploy v1.2 + exec rotation | open | v1.2 deployed `3880413`, prod reachable, live smoke 200 | **deploy DONE**; rotation half → see #12 |
| #12 ROTASI SECRET (3 secrets, "jalankan di VPS") | open/URGENT | JWT_PRIVATE_KEY+JWKS rotated; self-hosted admin key + INSTANCE_SECRET **N/A on Cloud**; only AUTH_GOOGLE_SECRET left | **narrowed** → owner-gated, 1 secret |

→ **Nothing left for vps to execute** — all open ops items are either done or gated on Rahman (Google Console).

## 2. Live prod snapshot — `rare-toucan-552.convex.cloud`

Row counts (integers only, no PII pulled):

| Table | Count | | Table | Count |
|---|--:|---|---|--:|
| tenants | 3 | | comments | 12 |
| memberships | 10 | | suggestions | 7 |
| profiles | 7 | | suggestionVotes | 17 |
| users | 7 | | announcements | 4 |
| courses | 7 | | **lessonCompletions** | **21** |
| modules | 9 | | **courseCompletions** | **6** |
| lessons | 17 | | **quizAttempts** | **5** |
| quizzes | 6 | | siteSettings | **0** |
| resources | 11 | | | |

**Notable findings:**
- **Real user activity exists.** `lessonCompletions` 21 / `courseCompletions` 6 / `quizAttempts` 5 are **non-zero and were never seeded** (seed deliberately skipped activity tables). Seed members have no auth identity, so this is genuine logged-in usage (owner testing and/or real learners). Analytics aggregates now read real data — do not fake/purge these.
- **`siteSettings` = 0.** No global platform-config row. Confirm the app tolerates defaults (it has so far); seed a config row only if a global setting actually needs a value.
- Flagship `belajar-ai` alive: resources 11 (9 flagship + 1 each on the two extra communities), comments 12, suggestions 7, votes 17. Silabus "Sumber belajar" board no longer lands empty.

## 3. Board drift — rows for alpha to reconcile in `docs/STATUS.md`

vps already updated the ops rows it owns (#12, #19, #14 note, B1 runbook, #11 annotation) + appended a drift-log row. If you want different wording, the source of truth is this report. Nothing else on the board is factually wrong.

## 4. Rotation #12 — final status (env NAMES only)

| State | Secret |
|---|---|
| ✅ rotated | `JWT_PRIVATE_KEY`, `JWKS` (done on prod; logged out active sessions) |
| ⊘ obsolete / N/A on Cloud | self-hosted admin key (`CONVEX_SELF_HOSTED_ADMIN_KEY`), `INSTANCE_SECRET`, `CONVEX_SELF_HOSTED_URL` |
| ⏳ pending — **owner only** | `AUTH_GOOGLE_SECRET` (Google Cloud Console reset → `npx convex env set AUTH_GOOGLE_SECRET … --prod` → verify `/masuk` login) |

The original #12 framing ("jalankan di VPS", regen admin key, restart backend) is void — there is no VPS backend. One owner step remains.

## 5. Doc drift — self-hosted → Cloud

**vps applied:** `docs/DEPLOY.md` — full reconcile to Convex Cloud (title, arch diagram, §A–§D deploy/env mechanics, redirect URI → `*.convex.site`). Old self-hosted runbook is in git history.

**alpha to apply** (contract/brand surfaces, vps out of scope per §4):

| File | Issue | Fix |
|---|---|---|
| `AGENTS.md` §2 | stack-lock pins "Convex self-hosted ≥1.16" (P1) | → "Convex Cloud (`rare-toucan-552`); self-hosted retired 2026-07-10". This is a stack-lock change — **your/Rahman's call.** |
| `AGENTS.md` §4 | vps role = owns "the Convex self-hosted stack" | rescope: no VPS backend to own; backend deploys to Cloud via CLI login / `CONVEX_DEPLOY_KEY`. Mark vps ops role largely dormant. |
| `AGENTS.md` §9 | quick-facts: "Convex self-hosted via Docker Compose on the same node" | → Convex Cloud. |
| `README.md` ~L107, L27, L72-75 | inverts reality: "self-hosted live (Cloud is the alt path)" | flip → "Convex Cloud live (`rare-toucan-552`); self-hosted retired". Update arch diagram + deploy blurb. |
| `docs/PRD.md` ~L186/194/195 | "Convex self-hosted" + manual deploy against self-hosted | note migration; deploy semantics unchanged but target Cloud. |

## 6. Files vps changed this handoff

- `docs/DEPLOY.md` — reconciled to Convex Cloud.
- `docs/STATUS.md` — ops rows #11/#12/#14/#19 + B1 runbook truthed; vps drift-log row added.
- `docs/reports/vps-2026-07-11.md` — this report.

No application source, no `convex/**`, no schema touched. No Convex deploy needed. Frontend rebuild on push is a no-op (identical bundle).

## 7. Open items

| Item | Owner | Action |
|---|---|---|
| `AUTH_GOOGLE_SECRET` rotation | **Rahman** | Google Console reset → `npx convex env set … --prod` → verify login. Rahman deferred; low urgency (secret was exposed in an old vps chat session — rotate when convenient). |
| AGENTS/README/PRD self-hosted facts | **alpha** | §5 edits. |
| `siteSettings` empty | alpha/owner | confirm defaults suffice, or seed a config row. |

## 8. Recommendation

Product/data side is healthy and alive — the only debt is **documentation lag** (docs still narrate a dead self-hosted stack) and **one owner-gated secret**. Apply §5, do the Google secret when convenient, and the repo's docs match production reality again.
