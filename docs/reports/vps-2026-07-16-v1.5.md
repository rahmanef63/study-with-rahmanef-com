# vps report — row #34 · deploy v1.5 seed + smoke (3 kursus baru)

**Date:** 2026-07-16 · **Agent:** vps · **Backend:** Convex Cloud `rare-toucan-552`
(template-projects/study-with-rahmanef-com) · **Domain:** https://study-with.rahmanef.com

## Status per langkah

| # | Langkah | Hasil |
|---|---------|-------|
| 1 | `git pull --ff-only origin main` | ✅ `54280bf..ec39d87` fast-forward; 3 modul seed + docs landed |
| 2 | `npx convex deploy` prod | ✅ dry-run confirm target `rare-toucan-552`; deploy `--yes` → **"No indexes are deleted"**, schema validation complete, tsc pass |
| 3 | 3 seed `--prod` (idempoten) | ✅ semua sukses; re-run seedAiKerja → `skipped: course already seeded` (idempoten dikonfirmasi) |
| 4 | Smoke rute + search | ✅ 3 rute 200; search member-authed memuat hasil per judul baru; anon guard `NOT_AUTHENTICATED` |
| 5 | Laporan | ini |

## Row counts per kursus (DB truth — seed return + `getOverview` reconcile)

| Kursus (slug) | Modul | Lesson | Kuis |
|---------------|:-----:|:------:|:----:|
| `ai-produktivitas-kerja` — "AI untuk Produktivitas Kerja" | 5 | 14 | 4 |
| `analisis-data-dengan-ai` — "Analisis Data dengan AI …" | 5 | 14 | 4 |
| `orkestrasi-multi-agent` — "Orkestrasi Multi-Agent untuk Proyek Nyata" | 5 | 14 | 4 |
| **Total baru** | **15** | **42** | **12** |

`getOverview(ai-produktivitas-kerja)` = `{modules:5, lessonCount:14}` — cocok dengan angka return seed & konten rows #31–#33. Tenant `belajar-ai` kini **6 kursus published** (3 lama + 3 baru), verifikasi via `courses:listPublished` (anon etalase).

## Smoke — rute UI (HTTP, prod live)

```
200  /kelas/belajar-ai/ai-produktivitas-kerja
200  /kelas/belajar-ai/analisis-data-dengan-ai
200  /kelas/belajar-ai/orkestrasi-multi-agent
```
Semua mounted 200 tanpa crash (shell OS catch-all render).

## Smoke — backend `searchInTenant` (member-authed via `--identity` impersonation, owner subject `<userId>|smoke-vps`)

| q | course | lesson | resource |
|---|:------:|:------:|:--------:|
| `Produktivitas` | 1 (ai-produktivitas-kerja) | 0 | 0 |
| `Analisis Data` | 1 (analisis-data-dengan-ai) | 15* | 0 |
| `Orkestrasi` | 1 (orkestrasi-multi-agent) | 0 | 0 |
| `Multi-Agent` | 1 (orkestrasi-multi-agent) | 13 | 0 |
| `AI` | 4 | 15* | 3 |

Setiap judul baru dimuat oleh term khasnya → search index (`courses.search_title` / `lessons.search_content`) menyerap konten baru ✅.
`*` = 15 = `LESSON_TAKE` cap (bounded index read, by design — bukan defect).
**Anon guard:** call `searchInTenant` tanpa identity → `NOT_AUTHENTICATED` (P0 authz-first tetap fires pasca-deploy) ✅.

## Mechanical hotfix (§4 vps exception — flag alpha)

`convex/_generated/api.d.ts`: deploy me-regen typed api → +6 baris additif (import + `typeof` untuk seedAiKerja/seedAnalisisData/seedMultiAgent; **bukan** `AnyApi` stub). Repo `api.d.ts` sebelumnya stale (3 modul baru absen) → tanpa commit ini, tsc/deploy agen lain akan gagal (pola row #26). Di-commit bareng report ini. **alpha post-review + confirm.**

## Proposals

- **[vps → alpha]** Changelog v1.5: 3 kursus baru sudah live (renderable oleh UI existing via `listPublished`) tapi belum ada entry di `slices/os-shell/changelog-data.ts`. Standing rule "changelog on every update" → alpha tambah entry Bahasa-Indonesia (3 kursus baru) saat wave v1.5 UI dibuka. vps tidak sentuh changelog (di luar kontrak deploy).
- **[vps → tooling]** Ulangi usul lama (belum diterapkan): CI/pre-commit codegen-freshness check (`convex codegen && git diff --exit-code convex/_generated`) supaya `api.d.ts` stale ketahuan **sebelum** deploy, bukan direpair vps tiap wave.
- **Tidak ada data user disentuh.** Seed idempoten (skip kalau course sudah ada); impersonation search = read-only query; `convex data` = read-only.
