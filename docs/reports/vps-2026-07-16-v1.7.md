# vps report — row #39 · deploy v1.7 (index by_user + recents; asisten DORMAN) + smoke

**Date:** 2026-07-16 · **Agent:** vps · **Backend:** Convex Cloud `rare-toucan-552`
(template-projects/study-with-rahmanef-com) · **Domain:** https://study-with.rahmanef.com

## Status per langkah

| # | Langkah | Hasil |
|---|---------|-------|
| 1 | `git pull --ff-only origin main` | ✅ fast-forward; asisten (#35) + progress/recents + ci.yml landed |
| 2 | `npx convex deploy` prod | ✅ target confirm `rare-toucan-552`; `--yes` → **"No indexes deleted"** + **`[+] lessonCompletions.by_user (userId, _creationTime)`** (additive), schema valid, tsc pass. **ANTHROPIC_API_KEY TIDAK di-set** (fitur AI diparkir owner) |
| 3 | Smoke UI + parkir | ✅ 3 rute 200 + /asisten dorman 200; dock/launcher/sidebar TIDAK memuat asisten (verified) |
| 4 | Smoke backend recents | ✅ recentCourses ber-identity → daftar kelas muncul (shape 4 kunci); anon → NOT_AUTHENTICATED |
| 5 | CI ci.yml first run | ✅ **HIJAU** (job `verify` 52s) |
| 6 | Laporan | ini |

## Deploy detail (row counts / index)

- `✔ No indexes are deleted by this push`
- `✔ Added table indexes: [+] lessonCompletions.by_user   userId, _creationTime` — **additive**, tidak ada index dihapus.
- Modul baru terdaftar: `features/progress/recents` (query) + `features/asisten/*` (chat/context/prompt/errors/validate) — **asisten DORMAN**: env `ANTHROPIC_API_KEY` **ABSENT** di prod (dicek via `convex env list --prod`, NAMES only). Tanpa env, action `ask` menjawab "belum aktif" (kill-switch = unset env, sesuai desain #35/#36).

## Smoke — rute UI (HTTP, prod live)

```
200  /                                          (Beranda)
200  /komunitas/belajar-ai
200  /kelas/belajar-ai/ai-produktivitas-kerja
200  /asisten                                   (deep-link DORMAN — render, bukan crash)
```

## Verifikasi parkir "Alfa"/asisten (dock/launcher/sidebar TIDAK memuat)

- **Satu seam** di `slices/os-shell/os-root.tsx`: `PARKED_APP_IDS = ["asisten"]` → `shellManifest.apps.filter(a => !PARKED_APP_IDS.includes(a.id) …)`. Komentar sumber: *"the ONE seam covering every shell's dock/launcher/sidebar (appshell rebuilds the registry from manifest.apps)"* → asisten dibuang dari registry ⇒ absen di **ketiga** permukaan. `slices/os-shell/nav-groups.ts` juga sengaja tidak mendaftarkan asisten (sidebar groups).
- SSR beranda: **0** kemunculan `asisten`/`Alfa`/`Tanya Alfa` (shell tipis, dock dirender klien dari manifest ter-filter).
- `/asisten` via URL langsung → **200** (routing deep-link terpisah dari app-registry; dorman, bukan crash).
- CI hijau atas commit yang sama (typecheck lolos pada kode parkir).

## Smoke — backend `progress:recentCourses` (member-authed via `--identity` impersonation)

Fn: `features/progress/recents:recentCourses` (args `{}`, `requireUser` auth-FIRST, index `by_user`, dedupe per course, RECENT_TAKE=6, filter published-course + active-tenant).

| User (real lessonCompletions) | Item | Shape |
|---|:---:|---|
| rahmanef63 (`mh72nm…`) | **3** | tiap item **4 kunci** `{tenantSlug,courseSlug,title,lastAt}` ✅ |
| Lennn (`mh7dkn…`) | **4** | idem ✅ |

- **Lintas komunitas** terbukti: item mencakup `karier-digital` + `belajar-ai` (satu user, banyak tenant) → "Lanjutkan belajar" lintas-device berfungsi (data ada di DB, device apa pun baca sama).
- **Anon guard:** call tanpa identity → `NOT_AUTHENTICATED` (P0 authz-first fires pasca-deploy) ✅.

## Langkah 5 — CI GitHub Actions (`ci.yml`, run pertama)

Run pertama (push `main`, "feat(v1.7): parkir fitur AI …") = **HIJAU** ✅. Job `verify` 52s:
`Install (lockfile)` ✓ · `Typecheck (tsc --noEmit)` ✓ · `Unit + convex-test (vitest)` ✓.
Trigger `on: push/pull_request → branches:[main]`. Repo publik ⇒ Actions gratis (row #38, alpha).
**Annotation non-blocking:** `Node.js 20 deprecated` — `actions/checkout@v4` + `actions/setup-node@v4` dipaksa jalan di Node 24. Warning saja, tidak menggagalkan run.

## Proposals

- **[vps → alpha, kecil]** Bump `actions/checkout@v4`→`@v5` + `actions/setup-node@v4`→`@v5` di `ci.yml` untuk hilangkan annotation deprecation Node 20 (kosmetik, tidak mendesak).
- **[vps → alpha, regression guard]** Belum ada e2e yang menegaskan dock/launcher/sidebar bebas-asisten. Parkir sekarang di-guard 1 seam sumber; kalau mau jaring regresi, tambah 1 assert Playwright (`await expect(dock).not.toContainText('Alfa')`) — kandidat, bukan blocker.
- **[vps → tooling, diulang]** Codegen-freshness di CI (`convex codegen && git diff --exit-code convex/_generated`) — row #38 catat butuh kredensial Convex (via `CONVEX_DEPLOY_KEY` read-only). Deploy v1.7 kembali menghasilkan drift KOSMETIK di `api.d.ts` (murni reorder alfabetis modul, semua modul sudah typed di main) → **di-revert vps, tidak di-commit** (bukan staleness, tidak blocking, CI hijau). Guard freshness akan menangkap drift semacam ini lebih awal.
- **Tidak ada data user disentuh.** recents = read-only query (impersonation), `convex data`/`env list` = read-only, no env di-set, no secret value di laporan.
