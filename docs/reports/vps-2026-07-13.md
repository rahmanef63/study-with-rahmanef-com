# vps → alpha handoff — deploy v1.3 (finale) + smoke

**Agent:** vps (AGENTS.md §4, Cloud-deploy dormant-role exception)
**Date:** 2026-07-13
**Row:** #26 (done) — depends #21–#25
**Commit:** `1aba552` (on `main`)
**Prod:** Convex Cloud `rare-toucan-552` · web `https://study-with.rahmanef.com`
**Secrets:** NAMES only below — no values.

---

## Per-step status

| # | Step | Result |
|---|------|--------|
| 1 | `git pull --ff-only origin main` | ✅ `b2da159 → 274c7f1` (v1.3 wave, alpha) |
| 2 | `npx convex deploy` → prod | ✅ `rare-toucan-552`. Schema **additive**: `notifications` table + `search_title`/`search_content` search indexes. "No indexes are deleted", "Schema validation complete", "Deployed Convex functions". **Blocked once** on a `tsc` error → hotfix (below) → clean re-deploy. |
| 3 | seed web-dev curriculum | ✅ `seedWebDev:seedWebDevContent {ownerEmail, tenantSlug:"belajar-ai"}` → course `bikin-aplikasi-web-dengan-ai`: **7 modul / 17 lesson / 4 kuis** (idempotent per course slug). |
| 4 | smoke UI (mounted routes) | ✅ **5/5 → 200, no crash.** |
| 5 | smoke backend | ✅ **4/4.** |
| 6 | remind #12 | ⚠️ `AUTH_GOOGLE_SECRET` rotation still **open, owner-gated** (Rahman, Google Console). |
| 7 | report | this file. |

Deploy target verified via `convex deploy --dry-run` **before** pushing: resolves to prod `rare-toucan-552` (this clone links dev `coordinated-finch-69` in `CONVEX_DEPLOYMENT`; `convex deploy` targets the project's prod regardless — dev deployment untouched). NB: `convex deploy` has **no `--prod` flag** (task text said `--prod`; correct invocation is bare `convex deploy` / `--yes`, which already targets prod). Ran with `--yes`, never `-v`.

---

## Smoke detail (9/9 green — parallel agents)

### UI — `study-with.rahmanef.com`, HTTP 200 + valid HTML doc + zero crash markers
(searched: "Application error" / "Internal Server Error" / "500" / "digest" / "Unhandled" / "This page could not be found" / "404")

- `/` ✅
- `/komunitas/belajar-ai` ✅
- `/kelas/belajar-ai/dasar-ai` ✅ (existing course)
- `/kelas/belajar-ai/bikin-aplikasi-web-dengan-ai` ✅ (**new seed** — dynamic `[slug]` route renders it, no rebuild needed)
- `/profil/rahman` ✅

Each is the ~5KB client-rendered OS-shell hydration doc (body renders client-side) — expected for this app, not an empty/error shell. **Not smoke-tested (per task — not yet mounted, row #27, alpha):** bell/inbox, SearchView, `/sertifikat/<id>`.

### Backend (`convex run … --prod`, `convex data … --prod`)
- **notifications table** — registered (`There are no documents in this table.`, 0 rows). ✅
- **publicGetCertificate (real completionId)** — returned exactly **5 keys, no id**: `displayName, username, courseTitle, tenantName, earnedAt`. Privacy projection holds. ✅
- **publicGetCertificate (bogus id)** — graceful `ConvexError {code:"NOT_FOUND","Sertifikat tidak ditemukan"}` via uniform `certificateNotFound()` (P0 §6), no stacktrace leak. CLI exit 1 is expected (convex non-zero on any thrown error). ✅
- **searchInTenant (unauth CLI)** — `ConvexError {code:"NOT_AUTHENTICATED","Silakan login dulu"}` from `requireUser`→`requireTenantRole` at handler line 1, before any db read. Proves fn deployed + P0 guard fires + no data leak. **Happy-path search must be exercised from the dashboard function runner** (member-authed) — CLI has no auth context. ✅

---

## Row counts (numbers only — no purge, no fake; real user activity present)

| Table | Rows | Note |
|-------|------|------|
| `courseCompletions` | many (real) | REAL user completions — source of the smoke completionId. **Do not touch.** |
| `notifications` | 0 | new table, no producers fired yet |
| `completions` | 0 | separate/legacy table (certificate reads `courseCompletions`, not this) |
| new course modules/lessons/quizzes | 7 / 17 / 4 | this deploy's seed |

---

## Mechanical hotfix in `1aba552` — **needs alpha confirm** (§4 exception)

1. **`convex/_generated/api.d.ts`** — committed `274c7f1` still held the **`AnyApi` fallback stub** (untyped), so the new v1.3 modules (notifications/search/seedWebDev/public) had no typed api — despite the 2026-07-13 drift-log claiming the typed variant was written. `convex deploy` regenerated the real typed api; committed it so `main` matches deployed prod + restores type safety.
2. **`convex/features/search/queries.test.ts:176`** — `long.snippet` accessed the `SearchHit` union (course variant has no `snippet`) + `long` possibly undefined → `tsc` failed → **deploy blocked**. Narrowed via type-predicate `(h): h is Extract<(typeof hits)[number], {kind:"lesson"}>` + `long!`. Test-only, behavior unchanged.
3. **Deliberately NOT committed:** `convex/_generated/server.d.ts` / `server.js` — my CLI (`1.42.1`) drops an unused `export const env = process.env` that a newer CLI emits. Committing = codegen downgrade churn; `git checkout`'d them. No code imports `env` from `_generated/server` (grep clean).

**Proposal (tooling):** pre-commit or CI **codegen-freshness gate** — `convex codegen` then `git diff --exit-code convex/_generated` — so a stale/`AnyApi` `api.d.ts` is caught before it reaches a deploy (this is the 2nd wave it's bitten: cf. #14, drift-log 2026-07-06).

---

## Open / for alpha

- **#27** (alpha, open) — mount bell+inbox (os-shell header), SearchView, deep-link `/sertifikat/<completionId>`, anon login-gate `resources-app` (zeta spec 9). Backend all live now; UI mount unblocked. After mount → flip fixme spec 8 + drop `test.fail` spec 9.
- **#12** (owner, open) — `AUTH_GOOGLE_SECRET` rotation (Google Console → `npx convex env set … --prod` → check `/masuk`). Low-urgency, Rahman-held.
- Confirm the mechanical hotfix `1aba552` (§4 post-review).
- Carry-over from `vps-2026-07-11.md` §5 — AGENTS.md §2/§4/§9 + README.md + docs/PRD.md self-hosted→Cloud reconciliation still pending alpha.
- **No CHANGELOG entry** this deploy: v1.3 features are backend-only until #27 mounts the UI — nothing user-visible yet. Alpha should prepend the Bahasa-Indonesia changelog entry (`slices/os-shell/changelog-data.ts`) in the same commit that mounts inbox/search/certificate.
