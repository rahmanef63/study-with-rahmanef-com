# vps → alpha handoff — deploy v1.4 (finale) + smoke

**Agent:** vps (AGENTS.md §4, Cloud-deploy dormant-role exception)
**Date:** 2026-07-16
**Row:** #30 (done) — depends #15, #28, #29
**Prod:** Convex Cloud `rare-toucan-552` · web `https://study-with.rahmanef.com`
**Secrets:** NAMES only below — no values.

---

## Per-step status

| # | Step | Result |
|---|------|--------|
| 1 | `git pull --ff-only origin main` | ✅ `7f697f1 → 2d01741` (v1.4 wave) |
| 2 | `npx convex deploy --yes` → prod | ✅ Dry-run FIRST confirmed target prod `rare-toucan-552` (clone links dev `coordinated-finch-69`; deploy targets project prod regardless). "No indexes are deleted", "Schema validation complete", "Deployed Convex functions". Schema union **additive** (`notifications.kind` + literal `"announcement"`). `tsc` passed during deploy — **no deploy-blocking hotfix needed this time.** |
| 3 | smoke UI (7 routes) | ✅ **7/7 → HTTP 200, zero crash markers.** Plus live Playwright anon suite: **8/9 green** (spec 8 = test-expectation noise, not an app defect — detail below). |
| 4 | smoke backend | ✅ announcement fan-out + searchInTenant happy-path + certificate contract + anon guard — all green (numbers below). |
| 5 | remind #12 | ⚠️ `AUTH_GOOGLE_SECRET` rotation still **open, owner-gated** (Rahman: reset in Google Console → `npx convex env set AUTH_GOOGLE_SECRET <val> --prod`). Env var NAME exists on prod; value is the pre-rotation one. |
| 6 | report | this file. |

Typed api regen: post-deploy `convex/_generated` diff = registers `features/announcements/notify` module in `api.d.ts` (was stale at `2d01741`) + drops the `env` export from `server.d.ts`/`server.js` (CLI 1.42.1 codegen artifact). Mechanical regen committed under the §4 hotfix exception — **flag alpha post-review**.

---

## Smoke detail

### UI — `study-with.rahmanef.com` (curl: HTTP 200 + no crash markers)

- `/` ✅
- `/komunitas/belajar-ai` ✅
- `/kelas/belajar-ai/bikin-aplikasi-web-dengan-ai` ✅
- `/profil/rahman` ✅ — NB: no profile row with handle `rahman` exists in prod (admin handle is `abdurrahman-fakhrul`); route still 200s and fails soft client-side.
- `/sertifikat/k178yhjtxgzkrzms4bx61wc0358a52rd` ✅ (real badge-backed completionId, dasar-ai)
- `/cari/belajar-ai` ✅
- `/resources/belajar-ai/usulan` ✅

Anon gates are client-rendered (not in SSR HTML), so gate copy verified via live Playwright run (`E2E_BASE_URL=https://study-with.rahmanef.com npx playwright test e2e/smoke.anon.spec.ts`): **8/9 green**, including spec 6 kelola gate, spec 9 usulan gate ("Masuk untuk membuka sumber & usulan") and the cari flow.

**Spec 8 (bogus `/sertifikat/<id>`) failed on its LAST assertion only:** the friendly not-found copy ("Sertifikat tidak ditemukan") **was visible** and `expectNoCrash` passed — but `collectErrors` collects `console.error`, and the Convex browser client always `console.error`s server-side ConvexErrors (plus `[profiles:PublicProfileBoundary] NOT_FOUND` from the boundary itself). Expected-by-design noise for an anon NOT_FOUND flow → the spec's `toEqual([])` can never pass against a real backend. **Not an app defect; UX contract holds.** Fix belongs to zeta/alpha (filter expected `ConvexError … NOT_FOUND` lines in spec 8's error collector) — filed in STATUS *Proposals*.

### Backend — prod `rare-toucan-552` (documented `npx convex run` entry points; member calls via `--identity` impersonation, sessionish subject `<userId>|smoke-vps`)

1. **Announcement fan-out (#28)** — created ONE test announcement via the real function `features/announcements/mutations:create` (impersonated as the belajar-ai **owner**; NOT a manual insert) → `announcementId` returned. Notifications table: **0 rows before → 5 rows `kind:"announcement"` after** (tenant has **6** members; sender excluded ⇒ 6−1=5 ✅), **5 distinct recipients**, **0 rows for the sender** (no-self-notify P0 ✅), href `/pengumuman/belajar-ai`. Discord leg: no-op by design (no tenant has `discordWebhookUrl` set). **No user data deleted** — the test announcement + 5 notification rows are real, harmless rows and remain (delete via dashboard if unwanted).
2. **searchInTenant happy-path (#23/#29)** — member-authed, `q:"AI"` → **2 course + 11 lesson + 3 resource** hits; the new "Sumber" group (`kind:"resource"`, approved-only — tenant has 9 approved resources) loads ✅.
3. **searchInTenant anon guard** — same call WITHOUT identity → `NOT_AUTHENTICATED` (P0 authz-first still fires post-deploy) ✅.
4. **publicGetCertificate (#24)** — real completionId → exactly **5 keys** (`courseTitle`, `displayName`, `earnedAt`, `tenantName`, `username`), **no ids** ✅; bogus id → uniform `NOT_FOUND` ("Sertifikat tidak ditemukan") ✅.

---

## Proposals (→ STATUS *Proposals*)

1. **e2e spec 8 console-error filter** (zeta/alpha): `collectErrors` in `e2e/smoke.anon.spec.ts` must whitelist the expected `ConvexError … NOT_FOUND` client log (and the `PublicProfileBoundary` log) for the bogus-certificate flow, or assert on crash markers only. As written the spec fails against any real backend while the UX contract passes.
2. **Profile handle mismatch** (alpha/owner): smoke route `/profil/rahman` targets a handle that doesn't exist in prod (`abdurrahman-fakhrul` is the real one). Either seed a `rahman` handle or update docs/e2e to the real handle so the profile smoke actually exercises a live profile.

## Open items

- **#12** `AUTH_GOOGLE_SECRET` rotation — owner-gated, unchanged.
