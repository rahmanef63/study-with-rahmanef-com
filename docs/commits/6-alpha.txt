feat(app): mount wave v1.1 - admin queue, request, profile, boards, quiz

Integrator mounts + board bookkeeping for wave v1.1 (all five slices done):
- /buka-komunitas (RequestTenantForm) + /admin -> /admin/komunitas
  (AdminTenantQueueView)
- /u/[username] (PublicProfileView + badge wall, shareUrl from site origin)
- /t/[slug]/resources + /usulan (boards, canModerate via membership) +
  /pengumuman (AnnouncementsView, canManage)
- /t/[slug] shell: nav tabs (Kelas/Resources/Usulan/Pengumuman)
- kelola editor page: per-module "Kelola quiz" entries + quiz builder route
  /t/[slug]/kelola/kelas/[courseId]/quiz/[moduleId]
- convex/_generated regenerated (loose fallback; typed api returns at the
  vps convex deploy - same cycle as #3)
- docs/STATUS.md: rows 6-10 done; drift log: mount-staleness escalation
  (truncated views, silent stale tracked files, git index corruption) +
  mitigation via this script-based integration

Verified pre-commit: npx tsc --noEmit 0 errors; vitest 273/273 green
(36 files; 106 new specs from wave v1.1).

Agent: alpha
Co-Authored-By: Claude <noreply@anthropic.com>
