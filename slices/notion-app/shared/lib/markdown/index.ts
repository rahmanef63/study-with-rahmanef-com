/** notion ⇄ markdown bridge — the sync wire format between the notion block
 *  editor and any plain-markdown surface (e.g. the `markdown` slice). */

export { blocksToMarkdown } from "./toMarkdown";
export { markdownToBlocks } from "./fromMarkdown";
