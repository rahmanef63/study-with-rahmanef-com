// notifications slice — UI mirrors of the server bounds
// (convex/features/notifications/validate.ts) — keep in sync.
export const UNREAD_TAKE = 30;
export const READ_TAKE = 20;
/** The badge renders copy.unreadOverflow ("99+") at this cap. */
export const UNREAD_COUNT_CAP = 99;
