// One boundary for every shelled account surface. The rail and the phone bar
// are in the layout above it, so only the content pane swaps.
export { AccountLoading as default } from "../_components/account-loading";
